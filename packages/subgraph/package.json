
{
  "name": "@neplus/subgraph",
  "version": "1.0.0",
  "description": "NEPLUS Subgraph for indexing Zora Factory and Creator Royalty events",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ neplus/neplus-zora",
    "create-local": "graph create --node http://localhost:8020/ neplus/neplus-zora",
    "remove-local": "graph remove --node http://localhost:8020/ neplus/neplus-zora",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neplus/neplus-zora"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
